
module.exports = {
    environment: ENVIRONMENT, // loaded from webpack config
    tradebaseServer: TRADEBASE_SERVER,  // loaded from webpack config
    tradebaseIISApp: IIS_APP_NAME, // loaded from webpack config
    tradebaseClient: CLIENT_NAME,        // loaded from webpack config
    tradebaseApi: API_NAME,        // loaded from webpack config
    googleTrackingID: GA_TRACKING_ID,
    docsLocation: DOCUMENTS_LOCATION,
};